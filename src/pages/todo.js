import React from "react";
import TodoComponent from '../containers/todo-container';

const ToDo = () => {
  return (
    <div className="todo-page-wrapper wrapper">
      <TodoComponent />
    </div>
  );
};

export default ToDo;